import { ClimaIconsPipe } from './clima-icons.pipe';

describe('ClimaIconsPipe', () => {
  it('create an instance', () => {
    const pipe = new ClimaIconsPipe();
    expect(pipe).toBeTruthy();
  });
});
